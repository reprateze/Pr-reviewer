[pytest]
# Garante que "app" seja importável nos testes independente de como o
# pytest é chamado (bare "pytest", "python -m pytest", CI, IDE, etc.).
# Sem isso, "pytest tests/" fora do modo "python -m" falha com
# ModuleNotFoundError: No module named 'app'.
pythonpath = .
